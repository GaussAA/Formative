# Formative 运维脚本

一组自动化运维脚本，用于管理 Formative 项目的开发、部署和维护。

## 📁 目录结构

```
scripts/ops/
├── index.js          # 主入口脚本
├── start.js          # 启动开发服务器
├── stop.js           # 停止开发服务器
├── clean.js          # 清理缓存和临时文件
├── reinstall.js      # 重装依赖
├── health.js         # 健康检查
└── README.md         # 本文件
```

## 🚀 快速开始

### 方式 1: 使用 pnpm 命令（推荐）

在项目根目录运行：

```bash
# 启动开发服务器
pnpm ops:start

# 停止开发服务器
pnpm ops:stop

# 健康检查
pnpm ops:health

# 清理缓存
pnpm ops:clean --all

# 重装依赖
pnpm ops:reinstall
```

### 方式 2: 直接运行脚本

```bash
node scripts/ops/index.js start
node scripts/ops/health.js --full
```

### 方式 3: 交互式菜单

```bash
node scripts/ops/index.js
# 或
pnpm ops
```

## 📋 脚本详情

### 1. 启动脚本 (start.js)

**功能**:

- 检查端口占用
- 验证环境变量
- 启动 Next.js 开发服务器
- 实时显示启动日志

**用法**:

```bash
pnpm ops:start
```

**特性**:

- ✅ 自动检测并终止占用端口的进程
- ✅ 验证 `.env.local` 配置
- ✅ 显示启动进度和访问地址
- ✅ 自动记录日志到 `logs/startup.log`
- ✅ 支持 Ctrl+C 平滑关闭

**输出示例**:

```
[22:30:01] ✅ 端口 3000 可用
[22:30:01] ✅ 环境变量检查通过
[22:30:02] 🚀 启动开发服务器...
[22:30:05] ✅ 开发服务器启动成功! (3.2s)
[22:30:05] 🌐 访问地址: http://localhost:3000
```

---

### 2. 关闭脚本 (stop.js)

**功能**:

- 查找并终止 Next.js 进程
- 释放端口
- 可选清理缓存

**用法**:

```bash
# 基本用法
pnpm ops:stop

# 终止并清理缓存
pnpm ops:stop --clean

# 强制终止（不询问）
pnpm ops:stop --force
```

**选项**:

- `--clean, -c`: 清理缓存文件
- `--cache`: 清理临时文件
- `--force, -f`: 不确认直接终止

**输出示例**:

```
[22:31:01] 找到 1 个进程:
[22:31:01]   PID: 12345 - next dev
[22:31:02] ✅ 已终止进程: 12345
[22:31:02] ✅ 端口已释放
```

---

### 3. 清理脚本 (clean.js) ⭐ 优化版

**功能**:

- ✅ 清理构建缓存 (`.next`, `.turbo`, `.swc`, `node_modules/.cache`)
- ✅ 清理日志文件 (`logs/` 目录)
- ✅ 清理测试报告 (`coverage/`, `playwright-report/`, `test-results/`)
- ✅ 清理临时文件 (`.DS_Store`, `Thumbs.db`, `nul`, `*.log`)
- ✅ 清理备份文件 (`.backup/`)
- ✅ 清理 TypeScript 构建信息 (`tsconfig.tsbuildinfo`)
- ✅ 清理临时文档
- ✅ 删除 node_modules 并重装依赖
- ✅ 显示磁盘使用情况

**用法**:

```bash
# 清理所有缓存和临时文件
pnpm ops:clean --all

# 仅清理构建缓存
pnpm ops:clean --cache

# 仅清理日志
pnpm ops:clean --logs

# 仅清理测试报告
pnpm ops:clean --test

# 仅清理临时文件
pnpm ops:clean --temp

# 删除 node_modules 并重装
pnpm ops:clean --reinstall

# 显示磁盘使用情况
pnpm ops:clean --disk

# 组合使用
pnpm ops:clean --cache --logs --test
```

**选项**:

- `--all, -a`: 清理所有缓存和临时文件
- `--cache, -c`: 仅清理构建缓存
- `--logs, -l`: 仅清理日志文件
- `--test, -t`: 仅清理测试报告
- `--temp`: 仅清理临时文件
- `--backup`: 仅清理备份文件
- `--ts`: 仅清理 TypeScript 构建信息
- `--docs`: 仅清理临时文档
- `--node-modules`: 删除 node_modules
- `--reinstall`: 删除并重装依赖
- `--npm`: 使用 npm 而非 pnpm
- `--disk, -d`: 显示磁盘使用情况
- `--help, -h`: 显示帮助信息

**清理内容**:

| 类别         | 目录/文件                                            | 说明                   |
| ------------ | ---------------------------------------------------- | ---------------------- |
| **构建缓存** | `.next/`, `node_modules/.cache/`, `.turbo/`, `.swc/` | Next.js/Turbo/SWC 缓存 |
| **测试报告** | `coverage/`, `playwright-report/`, `test-results/`   | 测试覆盖率和报告       |
| **日志文件** | `logs/` 目录所有文件                                 | 运行日志               |
| **备份文件** | `.backup/`                                           | 依赖备份               |
| **临时文件** | `.DS_Store`, `Thumbs.db`, `nul`, `*.log`             | 系统临时文件           |
| **TS 构建**  | `tsconfig.tsbuildinfo`                               | TypeScript 构建信息    |
| **临时文档** | `LOGGING_IMPLEMENTATION_COMPLETE.md`                 | 临时文档               |

**详见**: [CLEAN_USAGE.md](CLEAN_USAGE.md)

---

### 4. 重装脚本 (reinstall.js)

**功能**:

- 备份当前依赖状态
- 删除 node_modules 和 lock 文件
- 重新安装依赖
- 验证安装结果

**用法**:

```bash
# 自动检测包管理器并重装
pnpm ops:reinstall

# 指定包管理器
pnpm ops:reinstall --manager npm

# 安装后验证
pnpm ops:reinstall --verify

# 完整验证（包括构建）
pnpm ops:reinstall --build

# 强制执行（不询问）
pnpm ops:reinstall --force
```

**选项**:

- `--manager <name>`: 指定包管理器 (pnpm|npm|yarn)
- `--no-backup`: 跳过备份
- `--verify`: 安装后验证
- `--build`: 运行完整构建验证
- `--force, -f`: 不询问直接执行

**备份位置**: `.backup/backup-YYYY-MM-DD-HH-MM-SS/`

**输出示例**:

```
[22:32:01] ✅ 检测到 pnpm
[22:32:01] 备份当前依赖状态...
[22:32:01] ✅ 备份: package.json
[22:32:01] ✅ 备份: pnpm-lock.yaml
[22:32:01] 💡 备份位置: .backup/backup-2026-01-10-22-32-01
[22:32:02] 删除依赖文件...
[22:32:02] ✅ 已删除: node_modules
[22:32:02] ✅ 已删除: pnpm-lock.yaml
[22:32:03] 使用 pnpm 安装依赖...
[22:32:45] ✅ 依赖安装完成 (42.3s)
[22:32:45] 验证安装结果...
[22:32:45] ✅ 所有关键依赖已安装
```

---

### 5. 健康检查脚本 (health.js)

**功能**:

- 检查端口和进程状态
- 验证环境变量配置
- 检查依赖完整性
- 测试 API 端点
- 生成健康报告

**用法**:

```bash
# 标准检查
pnpm ops:health

# 完整检查（包括 API 测试）
pnpm ops:health --full

# 快速检查（跳过 API）
pnpm ops:health --quick

# JSON 格式输出
pnpm ops:health --json
```

**选项**:

- `--full, -f`: 执行完整检查（包括 API 测试）
- `--quick, -q`: 快速检查（跳过 API 测试）
- `--json`: 输出 JSON 格式报告

**检查项目**:

- ✅ 端口占用状态
- ✅ 进程运行状态
- ✅ 环境变量配置
- ✅ 依赖完整性
- ✅ 构建状态
- ✅ API 端点可用性

**输出示例**:

```
[22:33:01] 检查端口状态...
[22:33:01] ✅ 端口 3000 已被占用 (服务可能正在运行)
[22:33:01] 检查进程状态...
[22:33:01] ✅ 发现 1 个 Next.js 进程
[22:33:02] 验证环境变量...
[22:33:02] ✅ LLM_PROVIDER: deep***
[22:33:02] ✅ LLM_MODEL: deep***
[22:33:02] ✅ LLM_API_KEY: sk-***
[22:33:03] 测试 API 端点...
[22:33:03] ✅ / - 200 (45ms)
[22:33:03] ✅ /api/chat - 200 (123ms)

[22:33:03] ════════════════ 健康报告 ════════════════
[22:33:03] 通过: 6 | 警告: 0 | 失败: 0
[22:33:03] 🎉 完美! 系统健康状态良好
```

---

### 6. 主入口脚本 (index.js)

**功能**:

- 统一入口管理所有脚本
- 交互式菜单
- 系统信息展示

**用法**:

```bash
# 显示帮助
pnpm ops --help

# 显示菜单（交互式）
pnpm ops

# 直接运行命令
pnpm ops start
pnpm ops health --full

# 显示系统信息
pnpm ops info

# 显示项目状态
pnpm ops status
```

**交互菜单**:

```
╔════════════════════════════════════════════════════════════╗
║         Formative 运维菜单 - 请选择操作                    ║
╚════════════════════════════════════════════════════════════╝

  1. 启动开发服务器
  2. 停止开发服务器
  3. 健康检查
  4. 清理缓存
  5. 重装依赖
  6. 显示系统信息
  7. 显示项目状态
  0. 退出

请选择 (0-7):
```

---

## 📦 package.json 配置

在 `package.json` 中添加以下脚本：

```json
{
  "scripts": {
    "ops": "node scripts/ops/index.js",
    "ops:start": "node scripts/ops/start.js",
    "ops:stop": "node scripts/ops/stop.js",
    "ops:clean": "node scripts/ops/clean.js",
    "ops:reinstall": "node scripts/ops/reinstall.js",
    "ops:health": "node scripts/ops/health.js"
  }
}
```

---

## 🎯 使用场景

### 场景 1: 日常开发

```bash
# 1. 启动项目
pnpm ops:start

# 2. 开发过程中遇到问题
pnpm ops:health

# 3. 需要重启服务
pnpm ops:stop
pnpm ops:start
```

### 场景 2: 依赖问题

```bash
# 1. 检查健康状态
pnpm ops:health

# 2. 如果发现依赖问题
pnpm ops:reinstall --verify

# 3. 如果需要完整清理
pnpm ops:clean --reinstall
```

### 场景 3: 构建缓存问题

```bash
# 1. 清理缓存
pnpm ops:clean --all

# 2. 重新构建
pnpm build

# 3. 启动服务
pnpm ops:start
```

### 场景 4: 生产部署前检查

```bash
# 1. 健康检查
pnpm ops:health --full

# 2. 类型检查
pnpm type-check

# 3. 代码规范
pnpm lint

# 4. 构建验证
pnpm build
```

---

## 🔧 高级用法

### 自定义端口

修改脚本中的 `CONFIG.PORT` 常量：

```javascript
// scripts/ops/start.js
const CONFIG = {
  PORT: 3000, // 改为你想要的端口
  // ...
};
```

### 批量执行

```bash
# 一键重启
pnpm ops:stop && pnpm ops:start

# 一键清理并重启
pnpm ops:clean --all && pnpm ops:start

# 完整维护流程
pnpm ops:stop && pnpm ops:clean --all && pnpm ops:reinstall --verify && pnpm ops:start
```

### 定时任务

在 Linux/Mac 上可以使用 cron：

```bash
# 每天凌晨 2 点清理日志
0 2 * * * cd /path/to/formative && node scripts/ops/clean.js --logs

# 每周日检查健康状态
0 2 * * 0 cd /path/to/formative && node scripts/ops/health.js --full >> /var/log/formative-health.log
```

---

## 📊 日志管理

所有脚本都会在 `logs/` 目录生成日志：

- `logs/startup.log` - 启动日志
- `logs/app.log` - 应用运行日志（如果配置）

查看日志：

```bash
# Linux/Mac
tail -f logs/startup.log

# Windows
type logs\startup.log
```

清理日志：

```bash
pnpm ops:clean --logs
```

---

## 🛠️ 故障排除

### 问题 1: 端口被占用

```bash
# 手动释放端口
pnpm ops:stop --force

# 或使用系统命令
# Linux/Mac: kill -9 $(lsof -ti:3000)
# Windows: taskkill /F /PID $(netstat -ano | findstr ":3000")
```

### 问题 2: 依赖安装失败

```bash
# 1. 清理并重试
pnpm ops:clean --node-modules
pnpm ops:reinstall --verify

# 2. 如果仍然失败，尝试不同包管理器
pnpm ops:reinstall --manager npm --verify
```

### 问题 3: 构建失败

```bash
# 1. 完整清理
pnpm ops:clean --all

# 2. 删除缓存并重装
rm -rf node_modules/.cache
pnpm install

# 3. 重新构建
pnpm build
```

### 问题 4: 环境变量错误

```bash
# 1. 检查当前配置
pnpm ops:health

# 2. 参考模板创建
cp .env.example .env.local

# 3. 编辑 .env.local 填入正确的 API Key
```

---

## 📝 注意事项

1. **权限问题**: 某些操作可能需要管理员权限
2. **数据备份**: 重装依赖前会自动备份关键文件
3. **跨平台**: 脚本支持 Windows、Linux、macOS
4. **Node.js 版本**: 需要 Node.js 18+
5. **包管理器**: 默认使用 pnpm，可指定其他

---

## 🔗 相关文档

- [项目 README](../../README.md)
- [开发指南](../../docs/development.md)
- [部署文档](../../docs/deployment.md)

---

## 📞 支持

如有问题，请查看：

1. 脚本内置帮助: `node scripts/ops/index.js --help`
2. 项目文档
3. GitHub Issues

---

**版本**: 1.0.0
**更新日期**: 2026-01-10
**维护者**: Formative Team
