# 🚀 运维脚本快速启动指南

## 30 秒上手

### 安装后第一步

```bash
# 查看系统状态
pnpm ops:health
```

### 日常三连

```bash
pnpm ops:start    # 启动
pnpm ops:stop     # 停止
pnpm ops:health   # 检查
```

---

## 📱 常用命令速查表

| 场景 | 命令 | 说明 |
|------|------|------|
| **启动项目** | `pnpm ops:start` | 智能启动，自动处理一切 |
| **停止项目** | `pnpm ops:stop` | 安全关闭 |
| **重启项目** | `pnpm ops:stop && pnpm ops:start` | 一键重启 |
| **遇到问题** | `pnpm ops:health` | 诊断问题 |
| **清理缓存** | `pnpm ops:clean --all` | 释放空间 |
| **依赖问题** | `pnpm ops:reinstall` | 重装依赖 |
| **交互菜单** | `pnpm ops` | 可视化操作 |

---

## 🎯 场景化指南

### 场景 1: 第一次启动

```bash
pnpm ops:start
# ✅ 自动检查端口
# ✅ 验证环境变量
# ✅ 启动服务
# 🌐 显示访问地址
```

### 场景 2: 代码更新后

```bash
pnpm ops:clean --cache
pnpm ops:start
```

### 场景 3: 依赖冲突

```bash
pnpm ops:reinstall --verify
```

### 场景 4: 部署前检查

```bash
pnpm ops:health --full
pnpm build
```

---

## 💡 小贴士

### 快捷键
- `Ctrl + C` - 停止正在运行的服务
- `pnpm ops` - 打开交互菜单

### 日志位置
- 启动日志: `logs/startup.log`
- 备份文件: `.backup/`

### 帮助文档
- 详细文档: `scripts/ops/README.md`
- 快速指南: `scripts/ops/USAGE.md`
- 脚本帮助: `pnpm ops:health --help`

---

## 🔧 进阶用法

### 完整维护流程

```bash
# 一键完整维护
pnpm ops:stop && \
pnpm ops:clean --all && \
pnpm ops:reinstall --verify && \
pnpm ops:start
```

### 自定义端口

编辑 `scripts/ops/start.js`:
```javascript
const CONFIG = { PORT: 3000 }; // 改为你需要的端口
```

---

## ❓ 常见问题

**Q: 端口被占用怎么办？**
```bash
pnpm ops:stop --force
```

**Q: 启动失败怎么诊断？**
```bash
pnpm ops:health --full
```

**Q: 想要完整清理？**
```bash
pnpm ops:clean --all
```

**Q: 依赖安装失败？**
```bash
pnpm ops:reinstall --verify
```

---

## 📊 性能参考

- 启动: 2-10 秒
- 停止: < 1 秒
- 清理: 1-3 秒
- 重装: 30-60 秒
- 健康检查: 2-5 秒

---

**版本**: 1.0.0
**最后更新**: 2026-01-10
**状态**: ✅ 所有脚本已测试通过
